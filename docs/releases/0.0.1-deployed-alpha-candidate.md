# BoatOps 0.0.1 Deployed Alpha Candidate

- 验证时间：2026-08-04T03:45:25Z
- 当前候选：`20260804T034525Z`
- 已验证回滚候选：`20260804T034029Z`
- 状态：`ALPHA_CANDIDATE / CANDIDATE_DEPLOYED / NOT_RELEASED`
- 部署入口：`https://boatops.ayany.com`
- 权威发布身份：归档内 `RELEASE-MANIFEST.json` 与外置 final verification receipt

## 已完成技术门禁

- Inventory Provider API：7 个公开端点；Operations API 写操作要求 `operations.write`。
- Bearer Token 数据库仅存 SHA-256；组织上下文由认证中间件确定。
- HOLD、Booking、BLOCK、Trip、幂等、Outbox、audit、revision 和每分钟 HOLD 过期。
- Booking 私有价格快照；公开 API 与 Outbox 不含金额、汇率、成本、利润或客户字段。
- PostgreSQL 16 GiST 排斥约束、100 并发、三组资源竞态、备份恢复及恢复后约束验证。
- 本地与远端 PostgreSQL：`36 tests / 246 assertions`；Pint：43 files；契约：7 paths / 9 schemas。
- Availability 独立示例已由回归断言验证字段、正整数 ID 与时间顺序。
- DNS、TLS、HSTS、queue、scheduler、证书续期、390px 浏览器及两套 docs QA。
- 原子切换、回滚至 `20260804T034029Z`、再前滚均通过。

## 归档边界

- `.env`、Token、数据库、日志、vendor 和运行时 cache 不进入源码 manifest。
- `bootstrap/cache` 与 storage 空目录骨架保留；部署时 storage 挂载 shared 目录。
- 归档安装 production 依赖后，manifest 所列静态文件必须保持全部匹配。
- 归档 SHA-256 不能嵌入归档自身，记录在 detached checksum 和 final verification receipt。
- Git 基线：`c920043950e80a0a60ca88a83e440fc3b9882b94`；本候选含未提交的 availability 示例、回归测试及本说明修正。
- 本 agent 不执行 commit、push、Tag 或 GitHub Release。

## 仍需人工冻结

- 真实 BLOCK 原因、强制解除权限及审批策略。
- 真实运营 buffer、HOLD TTL、Plan B 时段及异常补录规则。
- 真实船员、岗位、证照及检查表。
- 真实售价、税费、佣金、汇率来源及 `RATE_CHANGED` 容差。
- 正式名称、商标、许可证、限流、日志留存、secret rotation、外部安全测试与真实迁移复核。
