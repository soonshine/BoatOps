{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://boatops.ayany.com/contracts/inventory-provider/v1/events/trip.completed.v1.schema.json",
  "title": "trip.completed.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["event_id", "event_type", "event_version", "occurred_at", "organization_id", "aggregate_type", "aggregate_id", "trip_id", "booking_id", "boat_id", "inventory_revision", "status"],
  "properties": {
    "event_id": {"type": "string", "format": "uuid"},
    "event_type": {"const": "trip.completed.v1"},
    "event_version": {"const": 1},
    "occurred_at": {"type": "string", "format": "date-time"},
    "organization_id": {"type": "integer", "minimum": 1},
    "aggregate_type": {"const": "trip"},
    "aggregate_id": {"type": "integer", "minimum": 1},
    "trip_id": {"type": "integer", "minimum": 1},
    "booking_id": {"type": "integer", "minimum": 1},
    "boat_id": {"type": "integer", "minimum": 1},
    "inventory_revision": {"type": "integer", "minimum": 1},
    "status": {"const": "COMPLETED"}
  }
}
